#include<stdlib.h>
//#include<math.h>
//#include<time.h>
//
//#define MAX(a,b) (a)>(b)?(a):(b)
//
//int main() {
//	int a = 10;
//	int b = 20;
//	printf("%d\n", MAX(a, b));
//
//	int a1 = 10;
//	int b1 = 20;
//	int c1 = 30;
//	printf("a, b 最大值: %d\n", a1 > b1 ? a1 : b1);
//	printf("a, b, c最大值: %d\n", a1 > b1 ? (a1 > c1 ? a1 : c1) : (b1 > c1 ? b1 : c1));
//	printf("a, b, c最大值: %d\n", (a1 > b1 ? a1 : b1) > c1 ?(a1 > b1 ? a1 : b1) : c1);
//	return 0;
//}